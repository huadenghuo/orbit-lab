---
authority_schema: tavernweave/total-design/v1
project_id: orbit-lab
title: 天体轨道实验室 Orbit Lab
status: design-approved
updated: 2026-09-24
next_gate: first-playable-acceptance
---

# 天体轨道实验室 Orbit Lab · 总设计案

## 目标与非目标

**一句话愿景**：零依赖、单文件、自研引力引擎的交互式轨道力学沙盒——拖出一颗星，看它画出椭圆；摆下三体，看混沌撕碎一切。

- 目标：延续科学沙盒系列（physics-lab / chemlab / evolution-sim / logic-sandbox）的「零依赖单文件 + 自研数值内核 + Node 测试 + Pages 上线」惯例，交付可玩的 N 体轨道实验室。
- 目标：把「积分器选型」做成可玩的对比实验（能量误差曲线），把「自研引擎、带测试」的人设落到可验证证据上。
- 非目标（首版明确不做）：3D/Three.js、Barnes-Hut N-body、相对论修正、URL 场景分享、洛希极限演示、开普勒面板、场景编辑器、CI、任何发布/推送/Git 动作。

## 项目类型与承载面

```yaml
recommended: independent-ui
fit: suitable
reason: 核心价值是「拖拽→演化→观察」实时交互，状态源明确（单一 state 对象），Canvas 擅长轨迹渲染，四件套已有同款宿主与部署链
fallback: 移动端性能不足时降 DPR、限子步、缩短拖尾，不换形态
reopenWhen: 需要 3D/WebGL 或多人实时协作时重新评估
driverOverride: false
```

## 用户价值与核心循环

目标用户：中学/大学物理生、科普爱好者、系列既有访客。打开即见「地月系统」自动运行，3 秒内理解这是什么。

核心循环：选/建天体 → 拖出发射矢量 → 演化观察（轨道拖尾 + 能量误差曲线）→ 调参/换积分器 → 重置或换预设。

## 四态决策账本

### 已确认
- D1 项目定位：orbit-lab，延续四件套（foundational，4/2，驾驶员两轮认可）。
- 决策① 积分器范围：引擎层三实现（半隐式欧拉/蛙跳 Leapfrog/RK4），UI 首版只放「积分器下拉 + 实时能量误差曲线」；不做 Wisdom–Holman 与调参面板。
- 决策② 单位制：归一化单位（G=1 为默认，中心质量≈1，长度以轨道半径为 1），UI 标注相对单位；首版不做双单位切换。
- First Playable 合同全文（见下），驾驶员已回「按蓝图开跑第一版」。

### 提案中
- D3 工程结构与发布链复用（eo-site 脚本、test 桩模式）——首版实施中验证，发布动作另需授权。

### 待决定
- 无（本轮 2 个决策已冻结）。

### 已否决
- 3D/Three.js、桌面壳（违背零依赖/四件套惯例）。
- 真实 SI 单位制（初版调试成本高，决策②已否决）。
- ecosystem-lab 式「憋大作」节奏（反面教材：快节奏 MVP → 上线 → 迭代）。
- D4 UI 视觉细节、D5 V2 功能占用本轮讨论（停入 Parking Lot）。

### 方向额度
| id | 方向 | level | roundLimit/used | status |
|---|---|---|---|---|
| D1 | 项目定位 | foundational | 4/2 | confirmed |
| D2 | 首版合同 | core | 3/2 | confirmed |
| D3 | 工程结构与发布 | standard | 2/1 | proposed |
| D4 | UI 视觉细节 | standard | 2/0 | parked |
| D5 | V2 功能 | peripheral | 1/0 | parked |

## Core Spine

- **权威状态源**：单一 state 对象 `{bodies[], G, eps, dt, timeScale, integrator, trails, camera}`；任何 UI 只读写它，不藏第二份状态。
- **核心对象**：天体 `{x,y,vx,vy,m,r,color,name,trail[]}`，两两牛顿引力 + 软化因子 ε（防零距离奇异）。
- **主循环**：rAF 驱动 `advance(h)` 子步推进 + 每帧渲染；暂停可单步。
- **集成器**：`INTEGRATORS = {euler, leapfrog, rk4}` 纯函数，能量/角动量/偏心率/合并/发散检测均为可单测的纯函数。
- **最低宿主**：零依赖单文件 HTML，双击即开，外部网络请求 = 0。

## First Playable / First Usable

- **入口与目标用户**：双击 `index.html`（上线后 `https://huadenghuo.github.io/orbit-lab/`）；打开即见地月系统。
- **主路径**：3 预设（地月椭圆 / 三体八字解 / 引力弹弓）→ 拖拽创建天体（速度箭头）→ 暂停/单步/0.1×–100× 时间缩放 → 滑杆调 G、dt、拖尾 → 切积分器看误差曲线 → 重置；平移/缩放视角，点选天体可删除。
- **失败路径**：① 数值发散（NaN/越界 1e5）→ 冻结 + toast「轨道已发散」+ 一键重置/关闭；② 低性能设备 → DPR 上限 2 + 子步上限 800/帧；③ 手机无 hover → 触屏 Pointer Events 统一处理，面板可收起。
- **必交付文件**：`index.html`、`test.mjs`、`README.md`（中英 + GIF 占位 + badges）、`总设计案.md`、`项目实现步骤蓝图.md`、`NEXT.md`、`.gitignore`。
- **明确不做**：见「非目标」。
- **验收证据四类**：
  - automated：`node test.mjs` 6 组断言全绿（蛙跳能量 <1e-6、欧拉误差 > 蛙跳、角动量守恒、逃逸速度 e≈1、八字解一周期回归 <1e-4、合并守恒 + NaN 防护）。
  - static-preview：单 script 标签、必需 DOM id 齐全、无控制台引导错误（Node 冒烟评估）。
  - real-host：桌面 Chrome + 375px 触屏实测 3 预设（**待驾驶员真机执行**）。
  - driver：驾驶员亲手试玩 → 提出第 1 次修改 → 完成并再试 → 明确验收（**待执行**）。
- **完成后停在哪里**：停在驾驶员验收，不自动滚入 Growth Tracks。

## Growth Tracks

| # | 内容 | 触发条件 |
|---|---|---|
| G1 | 开普勒 T²∝a³ 面板 | 首版验收通过 |
| G2 | 洛希极限撕裂演示 | 预设系统稳定 |
| G3 | URL 场景分享 | 上线后传播需求 |
| G4 | GitHub Actions CI + 拆 core/app（>2000 行） | 文件超阈值 |
| G5 | gh_profile 第五行 + Pages/EdgeOne 发布 | ✅ 已执行 2026-09-24：Pages 上线 + 主页第五行（驾驶员授权）；EdgeOne 未做，待另行授权 |

## Parking Lot

Barnes-Hut 星系、相对论近日点进动、带电粒子+磁场、3D/Three.js、桌面壳、Wisdom–Holman 显式辛积分、AI 一句话生成场景、UI 视觉细节专项（D4）。

## 稳定作品复用账本

| 产物 | 判定 | 证据 | 在本项目中的责任 |
|---|---|---|---|
| eo-site 发布脚本 | 复用 | 已读 gh_profile.mjs 源码（OWNER 常量） | 发布与主页同步（动作需单独授权） |
| test.mjs 测试桩模式 | 复用 | 已读 evolution-sim/test.mjs（抽 script + DOM 桩） | orbit-lab 测试骨架 |
| PLAN.md 计划书格式 | 复用 | 已读 evolution-sim/PLAN.md | 本文档沿用其「一句话愿景」惯例 |
| physics-lab step(h) 半隐式欧拉及「欧拉注入虚假能量→轨道螺旋发散」注释 | 仅参考 | 已读 index.html L212/L355–393 | 误差曲线叙事素材；场景不同不搬代码 |
| 四件套 README/命名/Pages 惯例 | 仅参考 | 总览笔记 | orbit-lab 对齐 |
| ecosystem-lab 憋大作模式 | 拒绝 | 几十条 probe 脚本未上线 | 反面教材 |
| CherryStudio/酒馆资产 | 拒绝 | 与本项目无关 | 不混入 |

## 前端适配结论

`independent-ui / suitable`，无劝退信号，无 driverOverride。

## 验收账本

| 门 | 状态 |
|---|---|
| automated | ✅ passed：`node test.mjs` 18/18 全绿（2026-09-24） |
| static-preview | ✅ passed：冒烟 7/7（单 script、30/30 id、0 网络请求、无 DOM 可引导） |
| real-host | pending（驾驶员真机） |
| driver | pending（驾驶员试用验收） |

## 下一道门

第一版实施完成 → 自动化与静态证据回填 → **驾驶员真机试用门**（双击 index.html 试 3 预设 + 给出第 1 次修改）。
